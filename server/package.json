{
  "name": "json-server",
  "version": "1.0.0",
  "description": "API Mock",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 8000 --host 0.0.0.0"
  },
  "keywords": ["frontend", "api", "mock"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "json-server": "^0.17.3"
  }
}
